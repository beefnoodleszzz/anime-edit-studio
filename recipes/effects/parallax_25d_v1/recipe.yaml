id: parallax_25d_v1
version: 0.1.0
kind: effect
engine: resolve_fusion
capability: parallax_25d
# UNVERIFIED: requires a GUI-built Fusion comp (subject-layer Transform faster
# than background Transform, single comp per the motion_phrase_compositor
# lesson) then Export + code Import + param-inject (A13), then a render check
# proving the parallax is visible with no edge tearing and identical frame
# count.  Until that exists this recipe must not enter an executable EditSpec.
verified: false
parameters:
  subject_push:
    type: float
    min: 0.0
    max: 0.2
    default: 0.08
  background_push:
    type: float
    min: 0.0
    max: 0.1
    default: 0.02
bindings:
  subject_push: "SubjectTransform.Size"
  background_push: "BackgroundTransform.Size"
